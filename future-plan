instrution not show in the patient pannel.
if doctor enable so a instrution will show in the patient pannel.
add a report and analytics in the admin and doctor panel.
add a chatbot to the patient pannel.
add a medical store panel for the medical store owner.
by default patient panel will be off 
if admin enable so a patient panel will show in the patient pannel.